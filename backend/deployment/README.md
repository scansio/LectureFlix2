Here we handle deployment concerning virtual private server i.e. server in which the project can run sudo and install services

All we need to do is

```bash
cd deployment
chmod +x install_service.sh
./install_service.sh
```
